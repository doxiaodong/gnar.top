{
  "name": "teemo",
  "version": "1.0.0-beta.0",
  "scripts": {
    "clean": "rm -rf dist",
    "check": "node check.js",
    "gulp": "gulp",
    "prebuild": "npm run check",
    "build": "npm run clean && npm run gulp"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-template": "^4.0.0"
  }
}
